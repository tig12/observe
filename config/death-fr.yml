
#
# 1 - Parameters common to all studies
#

# Unique id of this study
slug: death-fr

# fqcn = fully qualified class name ; the class handling this study
fqcn: observe\studies\death_fr\Death_fr

# Contains all intermediate files of this study
# Path relative to observe root directory, or absolute path
working-dir:  var/studies/death-fr

# Contains the final documents generated by the program
# Path relative to observe root directory, or absolute path
out-dir: output/studies/death-fr

# list of planets involved in this study (IAA codes)
planets: [SO, MO, ME, VE, MA, JU, SA, UR, NE, PL, NN]

# Description of the dates contained in data.csv.bz2
# The order of this array must correspond to the order of the columns in data.csv.bz2
dates:
  - birth
  - death

# Limit of p-value to consider that an anomaly is detected
p-value-limit: 0.05

# Precision (nb of digits for decimal part) of the numbers stored in expected distributions
expected-precision: 2

output:
  title: Deaths in France 1970 - 2025
  subtitle: 28 803 832 { birth, death }
  description: Statistical analysis of persons who died in France between 1970 and 2025.
  intro: |
    This study concerns persons who died in France between 1970 and 2025.
    <br>The data come from <a href="https://www.data.gouv.fr/datasets/fichier-des-personnes-decedees">data.gouv.fr/datasets/fichier-des-personnes-decedees</a>
    <br>See <a href="https://tig12.github.io/observe/studies/death-fr.html">tig12.github.io/observe/studies/death-fr.html</a> for more details.

#
# 2 - Parameters specific to this study
#

# Path to the sqlite database containing the data coming from data.gouv.fr
# Path relative to observe root directory, or absolute path
sqlite-death-fr: var/studies/death-fr/death-fr.sqlite3

# Path to the sqlite database containing temporary data to assist long command execution
# Path relative to observe root directory, or absolute path
sqlite-tmp: var/studies/death-fr/tmp.sqlite3

# When building import and controls, use a limit clause when reading the death-fr database.
# This avoids to load the whole database in memory and consume too much RAM (13 Go for the whole db)
import-limit: 1000
control-limit: 1000
