
config.yml
1.local

# files marked for deletion
z.*

# don't version data/ but version some subdirs
data/*
!data/sample

# don't version commands/ but version some subdirs
commands/*
!commands/sample

# tmp files generated by various tools
.~lock.*
\#*
